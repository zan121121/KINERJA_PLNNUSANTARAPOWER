<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <title>MLI Evaluasi PPK - e-Kinerja HC</title>
  <link rel="stylesheet" href="/css/output.css">
</head>
<body class="bg-gray-50">
  <%- include('../../partials/sidebar') %>
  <%- include('../../partials/topbar', { pageTitle: 'MLI Evaluasi PPK' }) %>

  <main class="ml-64 pt-16 p-6">
    <div class="bg-white rounded-xl shadow-sm border border-gray-100 p-6 mb-6">
      <div class="flex justify-between items-start flex-wrap gap-4">
        <div>
          <p class="text-lg font-bold text-gray-800">MLI Evaluasi PPK</p>
          <p class="text-gray-400 text-sm mt-0.5">Bukti dukung dan skor evaluasi program pengembangan</p>
        </div>
        <form method="GET" class="flex items-end gap-2">
          <select name="bulan" class="border border-gray-200 rounded-lg px-3 py-2 text-sm bg-gray-50 focus:outline-none focus:ring-2 focus:ring-brand-blue">
            <% ['Jan','Feb','Mar','Apr','Mei','Jun','Jul','Agu','Sep','Okt','Nov','Des'].forEach((nm, i) => { %>
              <option value="<%= i+1 %>" <%= (i+1) === bulan ? 'selected' : '' %>><%= nm %></option>
            <% }) %>
          </select>
          <input type="number" name="tahun" value="<%= tahun %>" class="border border-gray-200 rounded-lg px-3 py-2 text-sm bg-gray-50 w-24 focus:outline-none focus:ring-2 focus:ring-brand-blue">
          <button type="submit" class="bg-gray-800 text-white px-4 py-2 rounded-lg text-sm font-semibold hover:bg-gray-700">Tampilkan</button>
        </form>
      </div>

      <div class="mt-5 pt-5 border-t border-gray-100">
        <p class="text-xs text-gray-400">Rata-rata Skor Evaluasi</p>
        <p class="text-xl font-bold mt-0.5 <%= rataSkor >= 90 ? 'text-green-600' : rataSkor >= 70 ? 'text-yellow-600' : 'text-red-600' %>"><%= rataSkor %>%</p>
      </div>
    </div>

    <div class="flex justify-between items-center mb-4">
      <p class="font-bold text-gray-800 text-sm">Daftar Evaluasi Program</p>
      <% if (user.role === 'admin' || user.role === 'input') { %>
        <a href="/hcr-evaluasi/tambah?bulan=<%= bulan %>&tahun=<%= tahun %>" class="bg-brand-blue text-white px-4 py-2.5 rounded-lg font-semibold text-sm shadow-sm hover:opacity-90 flex items-center gap-1.5">
          <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2.2">
            <path stroke-linecap="round" stroke-linejoin="round" d="M12 4v16m8-8H4" />
          </svg>
          Tambah Evaluasi
        </a>
      <% } %>
    </div>

    <% if (successMsg && successMsg.length > 0) { %>
      <div class="bg-green-50 border border-green-200 text-green-700 text-sm px-4 py-3 rounded-lg mb-4"><%= successMsg %></div>
    <% } %>
    <% if (errorMsg && errorMsg.length > 0) { %>
      <div class="bg-red-50 border border-red-200 text-red-700 text-sm px-4 py-3 rounded-lg mb-4"><%= errorMsg %></div>
    <% } %>

    <div class="bg-white rounded-xl shadow-sm border border-gray-100 overflow-hidden">
      <table class="w-full text-sm">
        <thead class="bg-gray-50 text-left text-gray-500 text-xs uppercase tracking-wider">
          <tr>
            <th class="px-5 py-3 font-semibold">Nama Pegawai</th>
            <th class="px-5 py-3 font-semibold">Nama Program</th>
            <th class="px-5 py-3 font-semibold">Tgl Evaluasi</th>
            <th class="px-5 py-3 font-semibold">Skor</th>
            <th class="px-5 py-3 font-semibold">Bukti Dukung</th>
            <th class="px-5 py-3 font-semibold text-right">Aksi</th>
          </tr>
        </thead>
        <tbody class="divide-y divide-gray-100">
          <% dataEvaluasi.forEach(d => { %>
            <tr class="hover:bg-gray-50 transition">
              <td class="px-5 py-3.5 font-medium text-gray-800"><%= d.nama %></td>
              <td class="px-5 py-3.5 text-gray-600"><%= d.nama_program %></td>
              <td class="px-5 py-3.5 text-gray-600"><%= d.tanggal_evaluasi ? new Date(d.tanggal_evaluasi).toLocaleDateString('id-ID') : '-' %></td>
              <td class="px-5 py-3.5">
                <span class="font-semibold <%= d.skor_evaluasi >= 90 ? 'text-green-600' : d.skor_evaluasi >= 70 ? 'text-yellow-600' : 'text-red-600' %>">
                  <%= d.skor_evaluasi %>%
                </span>
              </td>
              <td class="px-5 py-3.5">
                <% if (d.file_bukti) { %>
                  <a href="/uploads/evaluasi-ppk/<%= d.file_bukti %>" target="_blank" class="text-blue-600 hover:underline text-sm font-medium">📎 Lihat File</a>
                <% } else { %>
                  <span class="text-gray-300">-</span>
                <% } %>
              </td>
              <td class="px-5 py-3.5 text-right space-x-3">
                <% if (user.role === 'admin' || user.role === 'input') { %>
                  <a href="/hcr-evaluasi/edit/<%= d.id %>" class="text-blue-600 hover:underline text-sm font-medium">Edit</a>
                <% } %>
                <% if (user.role === 'admin') { %>
                  <form action="/hcr-evaluasi/hapus/<%= d.id %>" method="POST" class="inline" onsubmit="return confirm('Yakin hapus data ini? File bukti juga akan ikut terhapus.')">
                    <button class="text-red-600 hover:underline text-sm font-medium">Hapus</button>
                  </form>
                <% } %>
              </td>
            </tr>
          <% }) %>
          <% if (dataEvaluasi.length === 0) { %>
            <tr><td colspan="6" class="px-5 py-10 text-center text-gray-400">Belum ada data untuk periode ini</td></tr>
          <% } %>
        </tbody>
      </table>
    </div>
  </main>
</body>
</html>