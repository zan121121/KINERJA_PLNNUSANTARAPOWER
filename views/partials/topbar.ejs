<header class="fixed top-0 left-64 right-0 h-16 bg-white border-b border-gray-200 flex items-center px-6 z-30">

  <div>
    <h1 class="text-lg font-bold text-gray-800">
      <%= typeof pageTitle !== 'undefined' ? pageTitle : 'Dashboard' %>
    </h1>
  </div>

</header>